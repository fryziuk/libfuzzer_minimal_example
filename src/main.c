#include <stdint.h>
#include <stdio.h>

int
LLVMFuzzerTestOneInput(const uint8_t *data, size_t size) {
    // try start debug this from macOS Clion, doesn't work
    // lldb from terminal works ok
    return 0;
}
